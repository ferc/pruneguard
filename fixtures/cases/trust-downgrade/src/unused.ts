// No importers — should be flagged as unused.
// Under high unresolved pressure, the confidence on this finding
// should be lower than in a fully-resolved graph.
export function abandoned(): number {
  return -1;
}
