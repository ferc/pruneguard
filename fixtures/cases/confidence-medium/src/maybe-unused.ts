// Not directly imported, but could be loaded via dynamic require.
// Confidence should be medium due to unresolved neighbor specifiers.
export function pluginInit(): void {
  console.log("plugin initialized");
}
