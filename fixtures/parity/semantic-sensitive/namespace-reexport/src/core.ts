export function add(a: number, b: number): number {
  return a + b;
}

export function multiply(a: number, b: number): number {
  return a * b;
}

export function unusedHelper(): void {
  // this is never called through the namespace chain
}
