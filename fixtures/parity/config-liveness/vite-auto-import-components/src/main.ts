// Entry point — uses <TheHeader> and <TheFooter> in templates via auto-import.
// No explicit import statement is needed; the plugin resolves them at build time.
export function app() {
  return 'auto-imported components';
}
