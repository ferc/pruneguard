// CSS module imports sometimes appear without file extensions in webpack configs.
import colors from 'colors.module';
import typography from 'typography.module';
import fields from 'fields.module';

export const styles = { colors, typography, fields };
