import baseStyles from 'base';
import themeConfig from 'theme';
import layoutGrid from 'layout';
import vendorLib from 'vendor';

export const app = { baseStyles, themeConfig, layoutGrid, vendorLib };
