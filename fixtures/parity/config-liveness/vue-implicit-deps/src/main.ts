// User code does not directly import 'vue' — the Vue framework is consumed
// by @vitejs/plugin-vue during build and at runtime via the SFC compiler.
export function greet(name: string): string {
  return `Hello ${name}`;
}
