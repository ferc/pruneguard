// Vue/Solid auto-imported components appear as bare PascalCase identifiers.
// These are resolved at compile time and are NOT npm packages.
import VueLogo from 'VueLogo';
import ErrorComponent from 'ErrorComponent';
import Outlet from 'Outlet';
import HeadContent from 'HeadContent';
import TanStackRouterDevtools from 'TanStackRouterDevtools';

export { VueLogo, ErrorComponent, Outlet, HeadContent, TanStackRouterDevtools };
