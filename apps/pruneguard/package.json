{
  "name": "pruneguard-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build-napi": "napi build --cwd ../../ -p pruneguard --features napi --no-js",
    "build-napi-release": "napi build --cwd ../../ -p pruneguard --release --features napi --no-js",
    "build-js": "tsdown src-js/index.ts src-js/bin.ts --format esm --dts --out-dir ../../npm/pruneguard/dist",
    "build": "pnpm build-js && cargo run --bin generate_schemas"
  },
  "devDependencies": {
    "@napi-rs/cli": "catalog:",
    "tsdown": "catalog:",
    "vitest": "catalog:"
  }
}
